# Configuración del CMS - FUNCIONAL INMEDIATO
backend:
  name: test-repo

# Carpetas de medios
media_folder: static/img
public_folder: /img

# Configuración del sitio
locale: 'es'

# Colecciones - FUNCIONALES INMEDIATO
collections:
  - name: "blog"
    label: "📝 Artículos"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Fecha", name: "date", widget: "datetime"}
      - {label: "Contenido", name: "body", widget: "markdown"}

  - name: "pages"
    label: "📄 Páginas"
    files:
      - name: "about"
        label: "Sobre Mí"
        file: "content/pages/about.md"
        fields:
          - {label: "Título", name: "title", widget: "string"}
          - {label: "Contenido", name: "body", widget: "markdown"}